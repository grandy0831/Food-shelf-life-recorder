import 'package:flutter/material.dart';

class Map294Screen extends StatelessWidget {
  const Map294Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Second Floor - Map"),
      ),
      body: Center(
        child: Text(
          'Second Floor Map',
          style: TextStyle(fontSize: 24),
        ),
      ),
    );
  }
}
